#include <iostream>


int main() {
    /*
     * std::exit(error_code)
     * std::atexit([functions])
     * std::quick_exit()
     * std::at_quick_exit()
     * std::abort()
     * std::terminate() // used in case of exception handeling
     * // mostly explicitly
     */

}
