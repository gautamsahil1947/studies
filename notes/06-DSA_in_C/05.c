#include <stdio.h>

char * longestCommonPrefix(char ** strs, int strsSize);

int main() {
    char* strs[] = {"flower","flow","flight"};
}


char * longestCommonPrefix(char ** strs, int strsSize) {
    int maxCommon = 0;
    for (int i = 0; i < strsSize; ++i) {

    }
    return "";
}
